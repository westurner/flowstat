[app:flow]
use = egg:flow
reload_templates = true
debug_authorization = false
debug_notfound = false
debug_routematch = false
debug_templates = true
default_locale_name = en
# sqlalchemy.url = sqlite:///%(here)s/flow.db
sqlalchemy.url = mysql+mysqldb://ffilms:jSZS[TB9%2BOYwxDZ1@localhost:3306/ffilms
sqlalchemy.echo_pool = True
sqlalchemy.pool_timeout = 2
sqlalchemy.pool_size = 20

jinja2.directories = flow:templates
rdflib.connstr = host=localhost,user=ffilms,password=jSZS[TB9+OYwxDZ1,db=ffilms 
rdflib.virtuoso_connstr = DSN=VOS;UID=dba;PWD=dba;WideAsUTF16=Y

[pipeline:main]
pipeline =
    egg:WebError#evalerror
    tm
    flow

[filter:tm]
use = egg:repoze.tm2#tm
commit_veto = repoze.tm:default_commit_veto

[server:main]
use = egg:Paste#http
host = 0.0.0.0
port = 6543

# Begin logging configuration

[loggers]
keys = root, flow, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[logger_flow]
level = DEBUG
handlers =
qualname = flow

[logger_sqlalchemy]
level = INFO
handlers =
qualname = sqlalchemy.engine
# "level = INFO" logs SQL queries.
# "level = DEBUG" logs SQL queries and results.
# "level = WARN" logs neither.  (Recommended for production systems.)

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s][%(threadName)s] %(message)s

# End logging configuration
